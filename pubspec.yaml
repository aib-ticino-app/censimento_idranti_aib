name: idranti_aib
description: "Censimento punti idrici AIB"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.1.0
  geolocator: ^10.1.0
  supabase_flutter: ^2.0.0
  shared_preferences: ^2.2.2
  http: ^1.2.0
  url_launcher: ^6.2.4
  latlong2: ^0.9.1
  flutter_compass: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png